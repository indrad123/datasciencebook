# Appendix B Exercises

1. Match NumPy, pandas, Matplotlib, SciPy, statsmodels, scikit-learn, and PyTorch to their main roles.
2. When is a Python list more appropriate than a NumPy array?
3. Explain `shape`, `ndim`, and `dtype`.
4. For a product-by-week array, explain `mean(axis=0)` and `mean(axis=1)`.
5. What is broadcasting, and what risk does it create?
6. Why might changing an array slice change its source?
7. Give five checks for a pandas merge.
8. Distinguish `.loc[]` and `.iloc[]`.
9. Why should a filtered DataFrame sometimes be copied before modification?
10. What information belongs on an analytical plot?
11. Distinguish SciPy and statsmodels.
12. Distinguish statistical inference and predictive modelling APIs.
13. What does `fit()` mean for a scikit-learn transformer?
14. How does a `Pipeline` reduce leakage risk?
15. List the main stages in a PyTorch training iteration.
16. Why does setting a random seed not guarantee identical deep-learning results everywhere?
17. Distinguish JupyterLab, a kernel, `nbformat`, and `nbclient`.
18. What evidence is lost when formal pytest cannot run?
19. Give four risks when converting among arrays, frames, and tensors.
20. Design a library stack for an NRG weekly demand prediction and reporting workflow.
